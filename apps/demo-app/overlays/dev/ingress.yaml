apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: demo-app
  namespace: demo-app-dev
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`app-demo.sedecy-it.net`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: demo-app
          port: 80
